# maimai FiNALE → DX Upgrade Guide

Welcome. This is a placeholder home page — replace with your own `index.md`
content whenever you're ready. Drop additional chapter `.md` files into this
`docs/` folder and register them in `mkdocs.yml` under `nav:`. Oh yeah~

